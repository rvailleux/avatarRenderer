{
  "devDependencies": {
    "create-react-component-folder": "^0.3.7"
  },
  "scripts": {
    "start": "node ./server/bin/www",
    "debug": "npm run build && cd ./server/ && npm start",
    "build":  "cd ./client && npm run build",
    "install": "cd ./server && npm install && cd ../client && npm install"
  }
}
